# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
if (i == 1){  nmt_dat <- nmt_contris  }
else { nmt_dat <- append(nmt_dat, nmt_contris)  }
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
if (i == 1){  mt_dat <- mt_contris  }
else { mt_dat <- append(mt_dat, mt_contris)  }
}
View(mt_contri)
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
if (i == 1){  nmt_dat <- nmt_contris  }
else { nmt_dat <- append(nmt_dat, nmt_contris)  }
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
if (i == 1){  mt_dat <- mt_contris  }
else { mt_dat <- append(mt_dat, mt_contris)  }
}
print(nmt_dat)
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(mt_dat)
rtpois(20,3,0,4)
rtpois(200,3,0,4)
rtpois(200,3,0,4)
rtpois(1000,3,0,4)
rtpois(1000,8,0,4)
rtpois(1000,15,0,4)
rtpois(100000,15,0,4)
rtpois(1000,15,0,4)
dat_try <- hier_cc_sim(100,3,.50,.50,sigma_alpha, sigma_rho, sigma_omega)
View(dat_try)
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(mt_dat)
View(nmt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
pacman::p_load(extraDistr, tidyverse, truncnorm, TruncPoisson)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
pacman::p_load(extraDistr, tidyverse, truncnorm, dtpois)
pacman::p_load(extraDistr, tidyverse, truncnorm, cascsim)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(mt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(mt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
source("HIER_simulation_FUNC.R")
source("HIER_simulation_FUNC.R")
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
source("HIER_simulation_FUNC.R")
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
source("HIER_simulation_FUNC.R")
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
View(mt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
source("HIER_simulation_FUNC.R")
nmt_dat <- list()
mt_dat <- list()
for (i in 1:nsims){
# sample group difference for all params
dif_alpha <- runif(1,0,2)
dif_rho <- runif(1, 0, .75)
dif_omega <- runif(1, 0, .75)
# calculate group means
nmt_mu_alpha <- compute_mean(pop_alpha, dif_alpha)
mt_mu_alpha <- compute_mean(pop_alpha, dif_alpha, mt=T)
nmt_mu_rho <- compute_mean(pop_rho, dif_rho)
mt_mu_rho <- compute_mean(pop_rho, dif_rho, mt=T)
nmt_mu_omega <- compute_mean(pop_omega, dif_omega)
mt_mu_omega <- compute_mean(pop_omega, dif_omega, mt=T)
##### ---------- RUNNING THE FUNCTION  ------------ #####
nmt_contris <- hier_cc_sim(150, nmt_mu_alpha, nmt_mu_rho, nmt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
nmt_dat[[i]] <- nmt_contris
mt_contris <- hier_cc_sim(150, mt_mu_alpha, mt_mu_rho, mt_mu_omega, sigma_alpha, sigma_rho, sigma_omega)
mt_dat[[i]] <- mt_contris
}
View(nmt_dat)
